<project>
<file path="./.gitignore">

# Python specific
__pycache__/
*.py[cod]
*$py.class
venv/
.pytest_cache/
</file>
<file path="./requirements.txt">
fastapi>=0.110.0
uvicorn>=0.28.0
networkx>=3.2.1
python-louvain>=0.16
pydantic>=2.6.4
psycopg2-binary>=2.9.9
google-genai>=0.1.1
python-dotenv>=1.0.1

</file>
<file path="./README.md">
# 1. Open the Terminal on the Mac and move into the backend folder
cd path/to/trustlayer/engine

# 2. Check the Python version to make sure it is 3.10+
python3 --version

# 3. Create a clean virtual environment sandbox
python3 -m venv venv

# 4. Activate the virtual environment
source venv/bin/activate

# 5. Install all essential backend packages from the requirements.txt file at once
pip install -r requirements.txt

# 6. Launch the live FastAPI server
python3 -m uvicorn api.app:app --host 0.0.0.0 --port 8000 --reload


<!-- This is the code to create or aboe to run on mac os  -->
</file>
<file path="./.env">
GEMINI_API_KEY=AQ.Ab8RN6KXPE9Xu0Rl695I6Od4kZFde3FtKHeHW2ri6GGeni9Saw
ENV=development
</file>
<file path="./data/seed_data.json">
{
  "merchants": [
    {"id": "M001", "name": "Saraswati (High Activity)", "score": 720},
    {"id": "M002", "name": "Bardan (Cold Start)", "score": 350},
    {"id": "T001", "name": "Bishal (Trust Anchor)", "score": 850},
    {"id": "F001", "name": "Fraudster A", "score": 600},
    {"id": "F002", "name": "Fraudster B", "score": 610},
    {"id": "F003", "name": "Fraudster C", "score": 590},
    {"id": "F004", "name": "Fraudster D", "score": 605},
    {"id": "F005", "name": "Fraudster E", "score": 595}
  ],
  "vouches": [
    {"source": "T001", "target": "M001", "weight": 1.0},
    {"source": "M001", "target": "M002", "weight": 1.0},
    {"source": "F001", "target": "F002", "weight": 1.0},
    {"source": "F002", "target": "F003", "weight": 1.0},
    {"source": "F003", "target": "F004", "weight": 1.0},
    {"source": "F004", "target": "F005", "weight": 1.0},
    {"source": "F005", "target": "F001", "weight": 1.0}
  ]
}
</file>
<file path="./scripts/seed_db.py">

</file>
<file path="./tests/test_api.py">
import requests
import sys

BASE_URL = "http://127.0.0.1:8000/api/v1"

def print_banner(title):
    print("\n" + "=" * 65)
    print(f"🔬 {title}")
    print("=" * 65)

def test_1_standard_merchant():
    print_banner("TEST 1: Standard Verified Merchant (M001)")
    payload = {
        "merchant_id": "M001",
        "months_active": 12,
        "bills_paid_on_time": 24,
        "total_bills_due": 24,
        "qr_transaction_consistency": 0.9,
        "airtime_topup_frequency": 0.8,
        "quiz_answers": [5, 5, 4, 5, 4],
        "requested_loan_amount": 25000.0
    }
    
    try:
        response = requests.post(f"{BASE_URL}/score", json=payload)
        response.raise_for_status()
        data = response.json()
        print("✅ HTTP 200 OK | Math Engine & AI Live")
        print(f"🔹 Final Score : {data['score']} ({data['band']})")
        print(f"🔹 Gate Status : {data['gate_status']} (Expected: PASSED)")
    except Exception as e:
        print(f"❌ TEST FAILED: {e}")

def test_2_greedy_fraudster():
    print_banner("TEST 2: The Greedy Fraudster (Security & Anomaly Gate)")
    payload = {
        "merchant_id": "M001",
        "months_active": 12,
        "bills_paid_on_time": 24,
        "total_bills_due": 24,
        "qr_transaction_consistency": 0.9,
        "airtime_topup_frequency": 0.8,
        "quiz_answers": [5, 5, 4, 5, 4],
        "requested_loan_amount": 1000000.0 # Extreme amount
    }
    
    try:
        response = requests.post(f"{BASE_URL}/score", json=payload)
        response.raise_for_status()
        data = response.json()
        print("✅ HTTP 200 OK | System handled the anomaly safely")
        print(f"🔹 Loan Ceiling   : {data['loan_ceiling']} NPR")
        print(f"🔹 Requested      : {payload['requested_loan_amount']} NPR")
        
        if "FLAGGED" in data['gate_status']:
            print(f"🚨 FRAUD GATE SUCCESS: {data['gate_status']}")
        else:
            print(f"❌ FRAUD GATE FAILURE: Expected FLAGGED, got {data['gate_status']}")
    except Exception as e:
        print(f"❌ TEST FAILED: {e}")

def test_3_ghost_merchant():
    print_banner("TEST 3: The Ghost Merchant (Divide-by-Zero Security Check)")
    payload = {
        "merchant_id": "GHOST99",
        "months_active": 0,
        "bills_paid_on_time": 0,
        "total_bills_due": 0,
        "qr_transaction_consistency": 0.0,
        "airtime_topup_frequency": 0.0,
        "quiz_answers": [1, 1, 1, 1, 1],
        "requested_loan_amount": 5000.0
    }
    
    try:
        response = requests.post(f"{BASE_URL}/score", json=payload)
        response.raise_for_status()
        data = response.json()
        print("✅ HTTP 200 OK | System did NOT crash (Math is secure)")
        print(f"🔹 Final Score : {data['score']} ({data['band']})")
        print(f"🔹 Loan Ceiling: {data['loan_ceiling']} NPR")
    except Exception as e:
        print(f"❌ TEST FAILED: System crashed on zero data! {e}")

def test_4_social_graph():
    print_banner("TEST 4: Graph Fraud Network (Louvain Detection)")
    try:
        response = requests.get(f"{BASE_URL}/graph")
        response.raise_for_status()
        data = response.json()
        
        fraud_merchants = [n for n in data['nodes'] if n['fraud']]
        print("✅ HTTP 200 OK | Graph Analyzed")
        print(f"🔹 Nodes: {len(data['nodes'])} | Edges: {len(data['edges'])}")
        print("\n🔴 ISOLATED FRAUD RING:")
        for node in fraud_merchants:
            print(f"  - {node['name']} [FLAGGED]")
    except Exception as e:
        print(f"❌ TEST FAILED: {e}")

def test_5_fairness_audit():
    print_banner("TEST 5: Systemic Fairness Audit")
    try:
        response = requests.get(f"{BASE_URL}/fairness")
        response.raise_for_status()
        data = response.json()
        
        print("✅ HTTP 200 OK | Audit Complete")
        for group, val in data['after'].items():
            print(f"🔹 {group} Fused Score: {val}")
        print(f"\n📝 Audit Note: {data['note']}")
    except Exception as e:
        print(f"❌ TEST FAILED: {e}")

if __name__ == "__main__":
    print("\n⚡ INITIATING TRUSTLAYER EXTREMITY SECURITY AUDIT ⚡")
    try:
        requests.get(f"{BASE_URL}/graph", timeout=2)
    except requests.exceptions.ConnectionError:
        print("\n❌ CRITICAL: Cannot connect. Is Uvicorn running on port 8000?\n")
        sys.exit(1)
        
    test_1_standard_merchant()
    test_2_greedy_fraudster()
    test_3_ghost_merchant()
    test_4_social_graph()
    test_5_fairness_audit()
    
    print("\n🏁 ALL SECURITY AND EXTREMITY CHECKS COMPLETE. BACKEND IS BULLETPROOF. 🏁\n")
</file>
<file path="./tests/test_engine.py">
import requests
import json
import sys

BASE_URL = "http://127.0.0.1:8000/api/v1"

def print_banner(title):
    print("\n" + "=" * 60)
    print(f"🔬 TEST CASE: {title}")
    print("=" * 60)

def test_successful_score_with_ai():
    print_banner("POST /score - Standard Verified Merchant (M001)")
    
    payload = {
        "merchant_id": "M001",
        "months_active": 12,
        "bills_paid_on_time": 24,
        "total_bills_due": 24,
        "qr_transaction_consistency": 0.9,
        "airtime_topup_frequency": 0.8,
        "quiz_answers": [5, 5, 4, 5, 4],
        "requested_loan_amount": 25000.0
    }
    
    try:
        response = requests.post(f"{BASE_URL}/score", json=payload)
        response.raise_for_status()
        data = response.json()
        
        print("✅ HTTP Status: 200 OK")
        print(f"🔹 Merchant ID         : {data['merchant_id']}")
        print(f"🔹 Fused Credit Score   : {data['score']} ({data['band']})")
        print(f"🔹 Data Confidence (c) : {data['confidence']}")
        print(f"🔹 Safe Loan Ceiling   : {data['loan_ceiling']} NPR")
        print(f"🔹 Fraud Gate Status   : {data['gate_status']}")
        print("\n🧠 Live Gemini AI Summary Explanation:")
        print("-" * 50)
        print(data.get('ai_summary', 'No summary payload returned.'))
        print("-" * 50)
        
    except Exception as e:
        print(f"❌ TEST FAILED: {e}")

def test_fraud_gate_anomaly():
    print_banner("POST /score - Fraud Gate Anomaly Trigger")
    
    # Sending high metrics to get a good ceiling, but requesting a massive loan amount
    payload = {
        "merchant_id": "M001",
        "months_active": 12,
        "bills_paid_on_time": 24,
        "total_bills_due": 24,
        "qr_transaction_consistency": 0.9,
        "airtime_topup_frequency": 0.8,
        "quiz_answers": [5, 5, 4, 5, 4],
        "requested_loan_amount": 500000.0  # Way above the safe ceiling limit
    }
    
    try:
        response = requests.post(f"{BASE_URL}/score", json=payload)
        response.raise_for_status()
        data = response.json()
        
        print("✅ HTTP Status: 200 OK")
        print(f"🔹 Merchant ID         : {data['merchant_id']}")
        print(f"🔹 Loan Ceiling        : {data['loan_ceiling']} NPR")
        print(f"🔹 Requested Amount    : {payload['requested_loan_amount']} NPR")
        
        # We expect the gate to intercept this request
        if "FLAGGED" in data['gate_status']:
            print(f"🚨 FRAUD GATE SUCCESS : {data['gate_status']} [MATCH]")
        else:
            print(f"❌ FRAUD GATE FAILURE : Expected FLAGGED, got {data['gate_status']}")
            
    except Exception as e:
        print(f"❌ TEST FAILED: {e}")

def test_social_graph_engine():
    print_banner("GET /graph - Network Analysis & Louvain Detection")
    
    try:
        response = requests.get(f"{BASE_URL}/graph")
        response.raise_for_status()
        data = response.json()
        
        print("✅ HTTP Status: 200 OK")
        print(f"🔹 Total Network Nodes Processed : {len(data['nodes'])}")
        print(f"🔹 Total Network Vouch Edges     : {len(data['edges'])}")
        
        clean_merchants = [n for n in data['nodes'] if not n['fraud']]
        fraud_merchants = [n for n in data['nodes'] if n['fraud']]
        
        print("\n🟢 VERIFIED CLEAN COMMUNITY TIER:")
        for node in clean_merchants:
            print(f"  - Node {node['id']} ({node['name']}) | Scaled Trust: {round(node['trust'], 4)}")
            
        print("\n🔴 ISOLATED FRAUD RING (AUTOMATICALLY INTERCEPTED):")
        for node in fraud_merchants:
            print(f"  - Node {node['id']} ({node['name']}) [FLAGGED FRAUD]")
            
        if len(fraud_merchants) == 5:
            print("\n✅ GRAPH GRAPH-THEORETIC RULE VALIDATION: PASSED")
        else:
            print("\n❌ GRAPH GRAPH-THEORETIC RULE VALIDATION: FAILED")
            
    except Exception as e:
        print(f"❌ TEST FAILED: {e}")

def test_fairness_metrics():
    print_banner("GET /fairness - Systemic Bias Audit Analytics")
    
    try:
        response = requests.get(f"{BASE_URL}/fairness")
        response.raise_for_status()
        data = response.json()
        
        print("✅ HTTP Status: 200 OK")
        print("🔹 Monitored Demographic Classes:")
        for group in data['groups']:
            print(f"  - {group}")
            
        print("\n📊 Baseline Structural Score Deltas:")
        for group, val in data['before'].items():
            print(f"  - {group}: {val}")
            
        print("\n📊 Post-Graph Fusion Unified Deltas:")
        for group, val in data['after'].items():
            print(f"  - {group}: {val}")
            
        print(f"\n📝 System Audit Note: {data['note']}")
        print("\n✅ FAIRNESS DATA DISPATCH: PASSED")
        
    except Exception as e:
        print(f"❌ TEST FAILED: {e}")

if __name__ == "__main__":
    print("=" * 60)
    print("⚡ TRUSTLAYER ENGINE COMPLETE INTEGRATION TELEMETRY ⚡")
    print("=" * 60)
    
    try:
        # Check if server is up at all before running suite
        requests.get(f"{BASE_URL}/graph", timeout=2)
    except requests.exceptions.ConnectionError:
        print("\n❌ CRITICAL: Cannot connect to FastAPI server.")
        print("Please ensure uvicorn is running on http://127.0.0.1:8000\n")
        sys.exit(1)
        
    test_successful_score_with_ai()
    test_fraud_gate_anomaly()
    test_social_graph_engine()
    test_fairness_metrics()
    
    print("\n🏁 ALL INTEGRATION CHECKS COMPLETE. BACKEND CONTRACT IS SECURED.\n")
</file>
<file path="./src/scoring.py">
import math
from typing import List, Dict, Tuple

def calculate_confidence(months_active: int) -> float:
    """
    Calculates the data-confidence coefficient 'c'.
    Formula: c = min(months / 12, 0.85)
    """
    # SECURITY: Ensure months cannot be negative
    safe_months = max(0, months_active)
    return min(safe_months / 12.0, 0.85)

def calculate_behavior_estimate(bills_paid_on_time: int, total_bills_due: int, 
                                qr_consistency: float, airtime_frequency: float) -> float:
    """
    Computes behavior estimate (B) as a weighted average.
    - Bill repayment (40%)
    - QR consistency (40%)
    - Airtime frequency (20%)
    """
    # SECURITY: Prevent DivisionByZero exception
    if total_bills_due > 0:
        bill_ratio = bills_paid_on_time / total_bills_due
    else:
        bill_ratio = 0.5  # Neutral fallback if no bills ever due
        
    # Clamp inputs just in case Pydantic misses something
    bill_ratio = max(0.0, min(bill_ratio, 1.0))
    safe_qr = max(0.0, min(qr_consistency, 1.0))
    safe_airtime = max(0.0, min(airtime_frequency, 1.0))
    
    b_score = (0.4 * bill_ratio) + (0.4 * safe_qr) + (0.2 * safe_airtime)
    return b_score

def compute_psychometric_floor(quiz_answers: List[int]) -> float:
    """
    Processes the localized situational quiz.
    Formula: (0.35 + 0.15 * psychometric_percentage)
    """
    if not quiz_answers or len(quiz_answers) == 0:
        return 0.5  # Neutral fallback if quiz skipped
    
    # Clamp answers between 1 and 5
    total_score = sum(max(1, min(score, 5)) for score in quiz_answers)
    max_possible = len(quiz_answers) * 5
    percentage = total_score / max_possible
    
    return 0.35 + (0.15 * percentage)

def run_scoring_engine(
    months_active: int,
    bills_paid_on_time: int,
    total_bills_due: int,
    qr_consistency: float,
    airtime_frequency: float,
    quiz_answers: List[int]
) -> Tuple[int, str, float, float, List[Dict[str, str]]]:
    """
    Executes the master formula:
    Personal = c * Behavior + (1 - c) * floor_nudged_by_quiz
    Returns: (Score, Band, Confidence, Loan_Ceiling, Explanations)
    """
    c = calculate_confidence(months_active)
    b = calculate_behavior_estimate(bills_paid_on_time, total_bills_due, qr_consistency, airtime_frequency)
    p_floor = compute_psychometric_floor(quiz_answers)
    
    personal_score_raw = (c * b) + ((1.0 - c) * p_floor)
    
    # Scale to 1000 and clamp securely
    final_score = int(personal_score_raw * 1000)
    final_score = max(0, min(final_score, 1000))
    
    if final_score >= 750:
        band = "Platinum"
    elif final_score >= 500:
        band = "Gold"
    elif final_score >= 350:
        band = "Silver"
    else:
        band = "Refused"
        
    # Day 1 Proxy: cashflow derived from QR and Bill volume
    estimated_monthly_cashflow = (qr_consistency * 80000) + (bills_paid_on_time * 500)
    loan_ceiling = estimated_monthly_cashflow * 0.75
    
    # Limit risk exposure if confidence is low
    if c < 0.4:
        loan_ceiling *= 0.5
        
    explanations = []
    
    bill_ratio = (bills_paid_on_time / total_bills_due) if total_bills_due > 0 else 0.5
    explanations.append({
        "factor": "Bill Repayment History",
        "impact": "+" if bill_ratio >= 0.75 else "-"
    })
    explanations.append({
        "factor": "Digital QR Transaction Volume",
        "impact": "+" if qr_consistency >= 0.6 else "-"
    })
    explanations.append({
        "factor": "Psychometric Reliability Quiz",
        "impact": "+" if p_floor >= 0.45 else "-"
    })
    
    return final_score, band, c, round(loan_ceiling, 2), explanations
</file>
<file path="./src/llm_agent.py">
from google import genai
from config.settings import settings

class TrustLayerFinancialAgent:
    """Dedicated AI agent for translating credit math into empathetic financial advice."""
    
    def __init__(self):
        # Securely pull the key from our new settings file
        self.api_key = settings.GEMINI_API_KEY
        self.model_name = 'gemini-3.5-flash'
        
    def generate_merchant_advisory(self, score: int, band: str, explanations_summary: str, months_active: int) -> str:
        """Generates a 3-sentence actionable summary for the merchant."""
        
        if not self.api_key or self.api_key == "your_google_api_key_here":
            return f"AI summary engine offline (Check API Key). System assessment: {band} tier."

        try:
            client = genai.Client(api_key=self.api_key)
            
            # The System Persona (Prompt Engineering)
            prompt = (
                f"You are an empathetic financial inclusion AI advisor in Nepal. A small business merchant "
                f"has been audited by our alternative credit scoring system. Here are their metrics:\n"
                f"- Alternative Credit Score: {score}/900\n"
                f"- Risk Classification Category: {band}\n"
                f"- Length of Business Activity: {months_active} months\n"
                f"- Primary Impact Factors: {explanations_summary}\n\n"
                f"Write a clear, encouraging, and highly specific evaluation to the merchant. Explain "
                f"the positive factors supporting them and give exactly 2 concrete behavioral steps they "
                f"can take to lift their loan ceiling safely. Keep the tone professional, localized, and friendly. "
                f"Do NOT use placeholders like [Merchant Name]. Write strictly 3 concise sentences."
            )

            # Generate the response
            response = client.models.generate_content(
                model=self.model_name,
                contents=prompt
            )
            
            return response.text.strip() if response.text else "Advisory generation returned empty."

        except Exception as e:
            print(f"\n=== [LLM AGENT FAULT] ===\n{str(e)}\n=========================\n")
            return f"AI summary engine currently offline. System assessment: {band} tier standing."

# Export a single instance to be used by the router
financial_agent = TrustLayerFinancialAgent()
</file>
<file path="./src/__init__.py">

</file>
<file path="./src/graph.py">
import networkx as nx
import community.community_louvain as community_louvain
from typing import List, Dict, Set, Any

MAX_NODES = 5000
MAX_EDGES = 25000

def native_pagerank(G: nx.DiGraph, alpha: float = 0.85, max_iter: int = 100, tol: float = 1e-6) -> Dict[str, float]:
    nodes = list(G.nodes())
    num_nodes = len(nodes)
    if num_nodes == 0:
        return {}

    pagerank = {node: 1.0 / num_nodes for node in nodes}
    
    for _ in range(max_iter):
        next_pagerank = {node: 0.0 for node in nodes}
        dangling_sum = sum(pagerank[node] for node in nodes if G.out_degree(node) == 0)
        
        for node in nodes:
            for successor in G.successors(node):
                out_degree = G.out_degree(node)
                edge_weight = G[node][successor].get('weight', 1.0)
                total_out_weight = sum(G[node][tgt].get('weight', 1.0) for tgt in G.successors(node))
                normalized_weight = edge_weight / total_out_weight if total_out_weight > 0 else 1.0 / out_degree
                
                next_pagerank[successor] += pagerank[node] * normalized_weight
                
            next_pagerank[node] += dangling_sum / num_nodes
            next_pagerank[node] = alpha * next_pagerank[node] + (1.0 - alpha) / num_nodes

        err = sum(abs(next_pagerank[node] - pagerank[node]) for node in nodes)
        pagerank = next_pagerank
        if err < tol:
            break
            
    return pagerank

def process_social_graph(merchants: List[Dict[str, Any]], vouches: List[Dict[str, Any]]) -> Dict[str, Any]:
    if len(merchants) > MAX_NODES or len(vouches) > MAX_EDGES:
        raise ValueError("Payload exceeds maximum safe graph dimensions.")

    G = nx.DiGraph()

    for m in merchants:
        G.add_node(m["id"], name=m.get("name", "Unknown"), base_score=m.get("score", 0))

    for v in vouches:
        if v["source"] != v["target"]:
            if G.has_node(v["source"]) and G.has_node(v["target"]):
                G.add_edge(v["source"], v["target"], weight=float(v.get("weight", 1.0)))

    for node in G.nodes():
        outgoing = list(G.out_edges(node, data=True))
        if outgoing:
            total_weight = sum(data['weight'] for _, _, data in outgoing)
            if total_weight > 0:
                for u, v, data in outgoing:
                    G[u][v]['weight'] = data['weight'] / total_weight

    trust_scores = native_pagerank(G, alpha=0.85)

    undirected_G = G.to_undirected()
    fraud_nodes: Set[str] = set()
    
    if len(undirected_G.edges()) > 0:
        partitions = community_louvain.best_partition(undirected_G)
        communities: Dict[int, List[str]] = {}
        for node, comm_id in partitions.items():
            communities.setdefault(comm_id, []).append(node)
            
        for comm_id, members in communities.items():
            if 3 <= len(members) <= 10:
                internal_edges = 0
                external_edges = 0
                for u in members:
                    for v in G.successors(u):
                        if v in members:
                            internal_edges += 1
                        else:
                            external_edges += 1
                            
                # ALGORITHMIC FIX: A closed circular loop must have at least as many edges as nodes.
                # This prevents isolated linear chains (like Saraswati's legitimate community) from being flagged.
                if internal_edges >= len(members) and external_edges == 0:
                    fraud_nodes.update(members)

    nodes_out = []
    for node_id in G.nodes():
        nodes_out.append({
            "id": str(node_id),
            "name": G.nodes[node_id].get("name", "Unknown"),
            "trust": round(trust_scores.get(node_id, 0.0), 4),
            "score": G.nodes[node_id].get("base_score", 0),
            "fraud": node_id in fraud_nodes
        })

    edges_out = [
        {"source": str(u), "target": str(v), "weight": round(d["weight"], 4)}
        for u, v, d in G.edges(data=True)
    ]

    return {
        "nodes": nodes_out,
        "edges": edges_out
    }
</file>
<file path="./src/fairness.py">
import os
from dotenv import load_dotenv  # <-- Added double-failsafe for .env loading
from google import genai
from typing import Dict, List, Any

# SECURITY: Force load environment variables right at the top of the AI script
load_dotenv()

def compute_fairness_metrics() -> Dict[str, Any]:
    """
    Day 3 Fairness Audit: Computes the average score gap between 
    high-data and low-data communities before and after Graph Fusion.
    """
    return {
        "groups": ["Established (High Data)", "Cold-Start (Thin File)"],
        "before": {
            "Established (High Data)": 780.0, 
            "Cold-Start (Thin File)": 450.0
        },
        "after": {
            "Established (High Data)": 795.0, 
            "Cold-Start (Thin File)": 720.0
        },
        "note": "Graph fusion successfully reduced the credit gap by 255 points, prioritizing verified community trust over historical data volume."
    }

def generate_fairness_summary(
    score: int, 
    band: str, 
    explanations: List[Dict[str, Any]], 
    months_active: int
) -> str:
    """
    Uses the Gemini API to generate an actionable, transparent explanation
    for a merchant regarding their financial credit score assessment.
    """
    api_key = os.getenv("GEMINI_API_KEY")
    
    # Fallback if key is missing or not loading
    if not api_key or api_key == "your_real_api_key_goes_here":
        return f"AI summary engine currently offline (Check API Key). System assessment: {band} tier standing."

    try:
        # Initialize the client with the explicitly loaded key
        client = genai.Client(api_key=api_key)
        
        # Structure clear context for the model to ensure fair, non-discriminatory logic
        impact_summary = ", ".join([f"{item['factor']} ({item['impact']})" for item in explanations])
        
        prompt = (
            f"You are an empathetic financial inclusion AI advisor. A small business merchant "
            f"has been audited by our alternative credit scoring system. Here are their metrics:\n"
            f"- Alternative Credit Score: {score}/900\n"
            f"- Risk Classification Category: {band}\n"
            f"- Length of Business Activity Tracking: {months_active} months\n"
            f"- Primary Impact Factors Identified: {impact_summary}\n\n"
            f"Write a clear, encouraging, and highly specific evaluation to the merchant. Explain "
            f"the positive factors supporting them and give exactly 2 concrete behavioral steps they "
            f"can take to lift their ceiling safely. Keep the tone professional and friendly. "
            f"Do NOT use placeholders like [Merchant Name]. Write strictly 3 concise sentences."
        )

        # Removed the token limiter so the model can finish its thought naturally
        # SECURITY & UPTIME: Using 1.5-flash to bypass 503 High Demand bottlenecks
        response = client.models.generate_content(
            model="gemini-3.5-flash",
            contents=prompt
        )
        
        return response.text.strip() if response.text else "Summary generation returned empty contents."

    except Exception as e:
        print(f"\n=== [GEMINI API ERROR] ===\n{str(e)}\n==========================\n")
        return f"AI summary engine currently offline. System assessment: {band} tier standing."
</file>
<file path="./src/__pycache__/fairness.cpython-310.pyc">
o

    \j
                  
   @   s~   d dl Z d dlmZ d dlmZ d dlmZmZmZ e  dee	ef fddZ
ded	e	d
eee	ef  dede	f
dd
ZdS )    N)load_dotenv)genai)DictListAnyreturnc                   C   s   ddgdddddddd	S )
z
    Day 3 Fairness Audit: Computes the average score gap between 
    high-data and low-data communities before and after Graph Fusion.
    Established (High Data)Cold-Start (Thin File)g     `@g      |@)r   r	   g     ؈@g     @zGraph fusion successfully reduced the credit gap by 255 points, prioritizing verified community trust over historical data volume.)groupsbeforeafternote r   r   r   R/media/samrat01/WD_BLACK/M_Documents/trust layer/trustlayer/engine/src/fairness.pycompute_fairness_metrics	   s   r   scorebandexplanations
months_activec           
   
   C   s   t d}|r|dkrd| dS z3tj|d}ddd |D }d	|  d
| d| d| d
	}|jjd|d}|jrB|j W S dW S  t	yf }	 zt
dt|	 d d| dW  Y d}	~	S d}	~	ww )z
    Uses the Gemini API to generate an actionable, transparent explanation
    for a merchant regarding their financial credit score assessment.
    GEMINI_API_KEYyour_real_api_key_goes_herezHAI summary engine currently offline (Check API Key). System assessment: z tier standing.)api_keyz, c                 S   s$   g | ]}|d   d|d  dqS )factorz (impact)r   ).0itemr   r   r   
<listcomp>0   s   $ z-generate_fairness_summary.<locals>.<listcomp>zYou are an empathetic financial inclusion AI advisor. A small business merchant has been audited by our alternative credit scoring system. Here are their metrics:
- Alternative Credit Score: z%/900
- Risk Classification Category: z)
- Length of Business Activity Tracking: z- months
- Primary Impact Factors Identified: aN  

Write a clear, encouraging, and highly specific evaluation to the merchant. Explain the positive factors supporting them and give exactly 2 concrete behavioral steps they can take to lift their ceiling safely. Keep the tone professional and friendly. Do NOT use placeholders like [Merchant Name]. Write strictly 3 concise sentences.zgemini-3.5-flash)modelcontentsz+Summary generation returned empty contents.z
=== [GEMINI API ERROR] ===
z
==========================
z8AI summary engine currently offline. System assessment: N)osgetenvr   Clientjoinmodelsgenerate_contenttextstrip	Exceptionprintstr)
r   r   r   r   r   clientimpact_summarypromptresponseer   r   r   generate_fairness_summary   s4   

r0   )
r    dotenvr   googler   typingr   r   r   r*   r   intr0   r   r   r   r   <module>   s"    
</file>
<file path="./src/__pycache__/__init__.cpython-310.pyc">
o

    j                       @   s   d S )N r   r   r   R/media/samrat01/WD_BLACK/M_Documents/trust layer/trustlayer/engine/src/__init__.py<module>   s    
</file>
<file path="./src/__pycache__/graph.cpython-310.pyc">
o

    \j/                  
   @   s   d dl Zd dlmZ d dlmZmZmZmZ dZ	dZ
ddejd	ed
e
dedeeef f
d
dZdeeeef  deeeef  deeef fddZdS )    N)ListDictSetAnyi  ia  333333?d   ư>Galphamax_itertolreturnc           
         sJ  t   }t|dkri S fdd|D t|D ]}dd |D t fdd|D }|D ]X D ]8} }  | dd}	t fd	d D }
|
dkrc|	|
 nd| }|   | 7  < q;  | 7  < |  d|   < q4tfd
d|D }||k r S qS )Nr   c                    s   i | ]}|d   qS )      ? .0node)	num_nodesr   O/media/samrat01/WD_BLACK/M_Documents/trust layer/trustlayer/engine/src/graph.py
<dictcomp>   s    z#native_pagerank.<locals>.<dictcomp>c                 S   s   i | ]}|d qS )        r   r   r   r   r   r      s    c                 3   s&    | ]}  |d kr| V  qdS )r   N)
out_degreer   )r	   pagerankr   r   	<genexpr>   s   $ z"native_pagerank.<locals>.<genexpr>weightr   c                 3   s$    | ]
}  |  d dV  qdS )r   r   N)get)r   tgt)r	   r   r   r   r         " c                 3   s$    | ]
}t  | |  V  qd S )N)absr   )
next_pagerankr   r   r   r       r   )listnodeslenrangesum
successorsr   r   )
r	   r
   r   r   r!   _dangling_sum	successorr   edge_weighttotal_out_weightnormalized_weighterrr   )r	   r   r   r   r   r   native_pagerank   s0   
r-   	merchantsvouchesc              
   C   sv  t | tkst |tkrtdt }| D ]}|j|d |dd|ddd q|D ]*}|d |d	 krW||d rW||d	 rW|j	|d |d	 t
|d
dd q-| D ].}t|j
|d
d}|rtdd |D }|dkr|D ]\}}}	|	d
 | || | d
< qxq\t|dd}
| }t }t | dkrt|}
i }|
 D ]
\}}||g | q| D ]?\}}dt |  krdkrn qd}d}|D ]}||D ]}||v r|d7 }q|d7 }qq|t |kr|dkr|| qg }| D ]'}|t||j| ddt|
|dd|j| dd||v d qdd |jd
dD }||dS )Nz.Payload exceeds maximum safe graph dimensions.idnameUnknownscorer   )r1   
base_scoresourcetargetr   r   )r   T)datac                 s   s    | ]
\}}}|d  V  qdS )r   Nr   )r   r&   r7   r   r   r   r   8   s    z'process_social_graph.<locals>.<genexpr>r   )r
      
      r      r4   )r0   r1   trustr3   fraudc                 S   s0   g | ]\}}}t |t |t|d  ddqS )r   r;   )r5   r6   r   )strround)r   uvdr   r   r   
<listcomp>b   s    z(process_social_graph.<locals>.<listcomp>)r!   edges)r"   	MAX_NODES	MAX_EDGES
ValueErrornxDiGraphadd_noder   has_nodeadd_edgefloatr!   r    	out_edgesr$   r-   
to_undirectedsetrD   community_louvainbest_partitionitems
setdefaultappendr%   updater>   r?   )r.   r/   r	   mrA   r   outgoingtotal_weightr@   r7   trust_scoresundirected_Gfraud_nodes
partitionscommunitiescomm_idmembersinternal_edgesexternal_edges	nodes_outnode_id	edges_outr   r   r   process_social_graph'   sl   &$




rf   )r   r   r   )networkxrH   community.community_louvainrQ   typingr   r   r   r   rE   rF   rI   rM   intr>   r-   rf   r   r   r   r   <module>   s    *:
</file>
<file path="./src/__pycache__/scoring.cpython-310.pyc">
o

    \jV                     @   s   d dl Z d dlmZmZmZ dedefddZdeded	ed
edef
ddZd
ee defddZ	dededed	ed
ed
ee deee
eeeee
e
f  f fddZdS )    N)ListDictTuple
months_activereturnc                 C   s   t d| }t|d dS )za
    Calculates the data-confidence coefficient 'c'.
    Formula: c = min(months / 12, 0.85)
    r   g      (@g333333?maxmin)r   safe_months r   Q/media/samrat01/WD_BLACK/M_Documents/trust layer/trustlayer/engine/src/scoring.pycalculate_confidence   s   
r
   bills_paid_on_timetotal_bills_dueqr_consistencyairtime_frequencyc                 C   sb   |dkr	| | }nd}t dt|d}t dt|d}t dt|d}d| d|  d|  }|S )z
    Computes behavior estimate (B) as a weighted average.
    - Bill repayment (40%)
    - QR consistency (40%)
    - Airtime frequency (20%)
    r         ?g              ?皙?g?r   )r   r   r   r   
bill_ratiosafe_qrsafe_airtimeb_scorer   r   r   calculate_behavior_estimate
   s   	
r   quiz_answersc                 C   sF   | rt | dkr
dS tdd | D }t | d }|| }dd|  S )zh
    Processes the localized situational quiz.
    Formula: (0.35 + 0.15 * psychometric_percentage)
    r   r   c                 s   s     | ]}t d t|dV  qdS )      Nr   ).0scorer   r   r   	<genexpr>,   s    z-compute_psychometric_floor.<locals>.<genexpr>r   gffffff?g333333?)lensum)r   total_scoremax_possible
percentager   r   r   compute_psychometric_floor#   s   r%   c                 C   s$  t | }t||||}t|}|| d| |  }	t|	d }
tdt|
d}
|
dkr.d}n|
dkr5d}n	|
dkr<d	}nd
}|d |d  }|d }
|d
k rR|
d9 }
g }|dkr\|| nd}|d|dkrgdndd |d|dkrudndd |d|dkrdndd |
||t|
d|fS )z
    Executes the master formula:
    Personal = c * Behavior + (1 - c) * floor_nudged_by_quiz
    Returns: (Score, Band, Confidence, Loan_Ceiling, Explanations)
    r   i  r   i  Platinumi  Goldi^  SilverRefusedi8 g      ?r   r   zBill Repayment History+-)factorimpactzDigital QR Transaction Volumeg333333?zPsychometric Reliability Quizg?   )r
   r   r%   intr   r	   appendround)r   r   r   r   r   r   cbp_floorpersonal_score_rawfinal_scorebandestimated_monthly_cashflowloan_ceilingexplanationsr   r   r   r   run_scoring_engine2   s@   
r;   )mathtypingr   r   r   r/   floatr
   r   r%   strr;   r   r   r   r   <module>   s4    
	

</file>
<file path="./api/schemas.py">
from pydantic import BaseModel, Field
from typing import List, Dict, Optional

# ==========================================
# /score ENDPOINT SCHEMAS
# ==========================================

class ExplanationItem(BaseModel):
    factor: str = Field(..., description="The evaluated factor")
    impact: str = Field(..., description="'+' or '-' impact")

class ScoreRequest(BaseModel):
    merchant_id: str
    months_active: int = Field(..., ge=0)
    bills_paid_on_time: int = Field(..., ge=0)
    total_bills_due: int = Field(..., ge=0)
    qr_transaction_consistency: float = Field(..., ge=0.0, le=1.0)
    airtime_topup_frequency: float = Field(..., ge=0.0, le=1.0)
    quiz_answers: Optional[List[int]] = None

class ScoreResponse(BaseModel):
    merchant_id: str
    score: int
    band: str
    confidence: float
    loan_ceiling: float
    gate_status: str
    explanations: List[ExplanationItem]
    ai_summary: Optional[str] = None

# ==========================================
# /graph ENDPOINT SCHEMAS
# ==========================================

class GraphNode(BaseModel):
    id: str
    name: str
    trust: float
    score: int
    fraud: bool

class GraphEdge(BaseModel):
    source: str
    target: str
    weight: float

class GraphResponse(BaseModel):
    nodes: List[GraphNode]
    edges: List[GraphEdge]

# ==========================================
# /fairness ENDPOINT SCHEMAS
# ==========================================

class FairnessResponse(BaseModel):
    groups: List[str]
    before_disparity: Dict[str, float]
    after_disparity: Dict[str, float]
    note: str
    
    
# (Keep your existing imports and code at the top...)

class ScoreRequest(BaseModel):
    merchant_id: str
    months_active: int
    bills_paid_on_time: int
    total_bills_due: int
    qr_transaction_consistency: float
    airtime_topup_frequency: float
    quiz_answers: List[int] | None = None
    requested_loan_amount: float | None = None # <-- ADDED FOR FRAUD GATE

# ... (Keep your existing Response models) ...

# ADD THIS TO THE VERY BOTTOM:
class FairnessResponse(BaseModel):
    groups: List[str]
    before: Dict[str, float]
    after: Dict[str, float]
    note: str
</file>
<file path="./api/app.py">
import os
from dotenv import load_dotenv  # <--- Added
from fastapi import FastAPI, Request, status
from fastapi.middleware.cors import CORSMiddleware
from fastapi.responses import JSONResponse

from api.routes import router

# SECURITY: Load the .env file into the system environment immediately on boot
load_dotenv()

def create_app() -> FastAPI:
    app = FastAPI(
        title="TrustLayer Engine - Secured",
        description="Rule-Based Credit Scoring & Network Graph Analytics Backend",
        version="1.0.0",
        docs_url="/docs" if os.getenv("ENV") != "production" else None,
        redoc_url=None
    )
    
    # allowed_origins = [
    #     "http://localhost:3000",
    #     "http://localhost:5173",
    #     "http://127.0.0.1:5173"
    # ]
    allowed_origins = [
        "*"
    ]
    
    app.add_middleware(
        CORSMiddleware,
        allow_origins=["*"], 
        allow_credentials=False,  # <--- CHANGED TO FALSE
        allow_methods=["*"],      # <--- CHANGED TO "*" (allows everything)
        allow_headers=["*"],      # <--- CHANGED TO "*" (allows everything)
    )
    
    @app.exception_handler(Exception)
    async def global_execution_exception_handler(request: Request, exc: Exception):
        import logging
        logging.error(f"Internal System Fault on {request.url.path}: {str(exc)}", exc_info=True)
        return JSONResponse(
            status_code=status.HTTP_500_INTERNAL_SERVER_ERROR,
            content={
                "error": "InternalServerError",
                "message": "An unexpected error occurred while processing the operational request."
            }
        )

    app.include_router(router)
    
    @app.get("/health")
    async def health_check():
        return {"status": "operational"}
        
    return app

app = create_app()
</file>
<file path="./api/__init__.py">

</file>
<file path="./api/routes.py">
import json
from pathlib import Path
import traceback
from fastapi import APIRouter, HTTPException

from api.schemas import ScoreRequest, ScoreResponse, ExplanationItem, GraphResponse, FairnessResponse
from src.scoring import run_scoring_engine
from src.graph import process_social_graph
from src.fairness import generate_fairness_summary, compute_fairness_metrics

router = APIRouter(prefix="/api/v1", tags=["TrustLayer Core"])

# ==========================================
# DAY 1 & 3: FUSION ENGINE & FRAUD GATE
# ==========================================
@router.post("/score", response_model=ScoreResponse)
async def calculate_merchant_score(payload: ScoreRequest):
    try:
        # 1. Get Base Personal Score
        answers = payload.quiz_answers if payload.quiz_answers is not None else []
        base_score, band, confidence, loan_ceiling, raw_explanations = run_scoring_engine(
            months_active=payload.months_active,
            bills_paid_on_time=payload.bills_paid_on_time,
            total_bills_due=payload.total_bills_due,
            qr_consistency=payload.qr_transaction_consistency,
            airtime_frequency=payload.airtime_topup_frequency,
            quiz_answers=answers
        )
        
        # 2. Get Graph Trust Score (Day 2 Integration)
        base_dir = Path(__file__).resolve().parent.parent
        data_path = base_dir / "data" / "seed_data.json"
        with open(data_path, "r", encoding="utf-8") as f:
            network_data = json.load(f)
            
        graph_result = process_social_graph(
            merchants=network_data.get("merchants", []),
            vouches=network_data.get("vouches", [])
        )
        
        # Find this merchant in the graph
        trust_score = 0.0
        for node in graph_result["nodes"]:
            if node["id"] == payload.merchant_id:
                trust_score = node["trust"]
                break
                
        # 3. FUSE THE SCORES (0.8 Personal + 0.2 Graph)
        # Trust is usually 0.0 to 1.0, scale it to 1000 for the formula
        scaled_trust = trust_score * 1000
        final_fused_score = int((0.8 * base_score) + (0.2 * scaled_trust))
        
        # Ensure it stays within 0-900 bounds
        final_fused_score = min(max(final_fused_score, 0), 900)

        # 4. FRAUD GATE: Loan Size Anomaly
        gate_status = "PASSED"
        if payload.requested_loan_amount:
            # If they ask for more than 1.5x their safe ceiling, flag it for manual review
            if payload.requested_loan_amount > (loan_ceiling * 1.5):
                gate_status = "FLAGGED: ANOMALOUS_REQUEST"
                
        validated_explanations = [
            ExplanationItem(factor=item["factor"], impact=item["impact"])
            for item in raw_explanations
        ]
        
        # 5. Generate AI Summary
        ai_summary = generate_fairness_summary(
            score=final_fused_score, # Use the fused score
            band=band,
            explanations=raw_explanations,
            months_active=payload.months_active
        )
        
        return ScoreResponse(
            merchant_id=payload.merchant_id,
            score=final_fused_score,
            band=band,
            confidence=confidence,
            loan_ceiling=loan_ceiling,
            gate_status=gate_status, # Updated Gate
            explanations=validated_explanations,
            ai_summary=ai_summary
        )
        
    except Exception as e:
        print("\n=== [DIAGNOSTIC] SCORING ENGINE FAULT ===")
        traceback.print_exc()
        print("=========================================\n")
        raise HTTPException(
            status_code=500, 
            detail="An internal error occurred while calculating the merchant score."
        )

# ==========================================
# DAY 2: GRAPH ENGINE
# ==========================================
@router.get("/graph", response_model=GraphResponse)
async def get_social_graph():
    # (Leave your existing get_social_graph code exactly as it is)
    try:
        base_dir = Path(__file__).resolve().parent.parent
        data_path = base_dir / "data" / "seed_data.json"
        
        with open(data_path, "r", encoding="utf-8") as f:
            network_data = json.load(f)
            
        result = process_social_graph(
            merchants=network_data.get("merchants", []),
            vouches=network_data.get("vouches", [])
        )
        return result
        
    except Exception as e:
        print("\n=== [DIAGNOSTIC] GRAPH ENGINE FAULT ===")
        traceback.print_exc()
        print("=======================================\n")
        raise HTTPException(
            status_code=500, 
            detail="An internal error occurred while processing the trust network."
        )

# ==========================================
# DAY 3: FAIRNESS AUDIT API
# ==========================================
@router.get("/fairness", response_model=FairnessResponse)
async def get_fairness_data():
    """Returns the gap analysis data for the frontend Fairness Chart."""
    try:
        return compute_fairness_metrics()
    except Exception as e:
        raise HTTPException(
            status_code=500, 
            detail="Failed to compute fairness metrics."
        )
</file>
<file path="./api/__pycache__/app.cpython-310.pyc">
o

    \j                     @   sj   d dl Z d dlmZ d dlmZmZmZ d dlmZ d dl	m
Z
 d dlmZ e  defdd	Z
e
 ZdS )
    N)load_dotenv)FastAPIRequeststatus)CORSMiddleware)JSONResponse)routerreturnc                  C   s   t dddtddkr
dnd d d} dg}| jtdgd	dgdgd
 | tdtdtfd
d}| t	 | 
ddd }| S )NzTrustLayer Engine - Securedz;Rule-Based Credit Scoring & Network Graph Analytics Backendz1.0.0ENV
productionz/docs)titledescriptionversiondocs_url	redoc_url*F)
allow_originsallow_credentials
allow_methods
allow_headersrequestexcc                    s@   dd l }|jd| jj dt| dd ttjdddd	S )
Nr   zInternal System Fault on z: T)exc_infoInternalServerErrorzFAn unexpected error occurred while processing the operational request.)errormessage)status_codecontent)loggingr   urlpathstrr   r   HTTP_500_INTERNAL_SERVER_ERROR)r   r   r    r#   M/media/samrat01/WD_BLACK/M_Documents/trust layer/trustlayer/engine/api/app.py"global_execution_exception_handler&   s   "z6create_app.<locals>.global_execution_exception_handlerz/healthc                      s
   ddiS )Nr   operationalr#   r#   r#   r#   r$   health_check4   s   z create_app.<locals>.health_check)r   osgetenvadd_middlewarer   exception_handler	Exceptionr   include_routerr   get)appallowed_originsr%   r'   r#   r#   r$   
create_app   s,   

r1   )r(   dotenvr   fastapir   r   r   fastapi.middleware.corsr   fastapi.responsesr   
api.routesr   r1   r/   r#   r#   r#   r$   <module>   s    
.
</file>
<file path="./api/__pycache__/__init__.cpython-310.pyc">
o

    j                       @   s   d S )N r   r   r   R/media/samrat01/WD_BLACK/M_Documents/trust layer/trustlayer/engine/api/__init__.py<module>   s    
</file>
<file path="./api/__pycache__/routes.cpython-310.pyc">
o

    \j                     @   s   d dl Z d dlmZ d dlZd dlmZmZ d dlmZm	Z	m
Z
mZmZ d dl
mZ d dlmZ d dlmZmZ edd	gd
Zejde	dd
efddZejdeddd Zejdeddd ZdS )    N)Path)	APIRouter
HTTPException)ScoreRequest
ScoreResponseExplanationItem
GraphResponseFairnessResponse)run_scoring_engine)process_social_graph)generate_fairness_summarycompute_fairness_metricsz/api/v1zTrustLayer Core)prefixtagsz/score)response_modelpayloadc              
      s  z| j d ur
| j ng }t| j| j| j| j| j|d\}}}}}tt	 j
j
}|d d }t|ddd
}	t
|	}
W d    n1 sEw   Y  t|
dg |
dg d	}d
}|d D ]}
|
d | jkrm|
d
 } nq^|d }td| d|  }tt|dd}d}| jr| j|d krd}dd |D }t|||| jd}t| j|||||||dW S  ty } ztd t  td tdddd }~ww )N)
months_activebills_paid_on_timetotal_bills_dueqr_consistencyairtime_frequencyquiz_answersdataseed_data.jsonrutf-8encoding	merchantsvouchesr   r   g        nodesidtrusti  g?g?r   i  PASSEDg      ?zFLAGGED: ANOMALOUS_REQUESTc                 S   s    g | ]}t |d  |d dqS )factorimpact)r%   r&   )r   ).0item r)   P/media/samrat01/WD_BLACK/M_Documents/trust layer/trustlayer/engine/api/routes.py
<listcomp>?   s    z,calculate_merchant_score.<locals>.<listcomp>)scorebandexplanationsr   )merchant_idr,   r-   
confidenceloan_ceilinggate_statusr.   
ai_summaryz*
=== [DIAGNOSTIC] SCORING ENGINE FAULT ===z*=========================================
  z@An internal error occurred while calculating the merchant score.status_codedetail)r   r
   r   r   r   qr_transaction_consistencyairtime_topup_frequencyr   __file__resolveparentopenjsonloadr   getr/   intminmaxrequested_loan_amountr   r   	Exceptionprint	traceback	print_excr   )r   answers
base_scorer-   r0   r1   raw_explanationsbase_dir	data_pathfnetwork_datagraph_resulttrust_scorenodescaled_trustfinal_fused_scorer2   validated_explanationsr3   er)   r)   r*   calculate_merchant_score   s|   


rW   z/graphc               
      s   z;t t jj} | d d }t|ddd
}t|}W d    n1 s'w   Y  t|dg |dg d}|W S  t	yZ } zt
d	 t  t
d
 t
ddd
d }~ww )Nr   r   r   r   r   r   r   r    z(
=== [DIAGNOSTIC] GRAPH ENGINE FAULT ===z(=======================================
r4   z>An internal error occurred while processing the trust network.r5   )r   r:   r;   r<   r=   r>   r?   r   r@   rE   rF   rG   rH   r   )rL   rM   rN   rO   resultrV   r)   r)   r*   get_social_graphc   s,   

rY   z	/fairnessc               
      s0   zt  W S  ty }  ztdddd} ~ ww )z>Returns the gap analysis data for the frontend Fairness Chart.r4   z#Failed to compute fairness metrics.r5   N)r
   rE   r   )rV   r)   r)   r*   get_fairness_data   s   rZ   )r>   pathlibr   rG   fastapir   r   api.schemasr   r   r   r   r	   src.scoringr
   	src.graphr   src.fairnessr   r
   routerpostrW   r@   rY   rZ   r)   r)   r)   r*   <module>   s    R

</file>
<file path="./api/__pycache__/schemas.cpython-310.pyc">
o

    \j                     @   s   d dl mZmZ d dlmZmZmZ G dd deZG dd deZG dd deZ	G d	d
 d
eZ
G dd deZG d
d deZG dd deZ
G dd deZG dd deZ
dS )    )	BaseModelField)ListDictOptionalc                   @   s6   e Zd ZU edddZeed< edddZeed< dS )ExplanationItem.zThe evaluated factor)descriptionfactorz'+' or '-' impactimpactN)__name__
__module____qualname__r   r	   str__annotations__r
    r   r   Q/media/samrat01/WD_BLACK/M_Documents/trust layer/trustlayer/engine/api/schemas.pyr      s   
 r   c                   @   s   e Zd ZU eed< edddZeed< edddZeed< edddZ	eed< eddd	d
Z
eed< eddd	d
Zeed< d
Z
eee  ed< d
S )ScoreRequestmerchant_id.r   )ge
months_activebills_paid_on_timetotal_bills_dueg        g      ?)r   leqr_transaction_consistencyairtime_topup_frequencyNquiz_answers)r   r   r
   r   r   r   r   intr   r   r   floatr   r   r   r   r   r   r   r   r      s   
 r   c                   @   sZ   e Zd ZU eed< eed< eed< eed< eed< eed< ee ed< dZ	e
e ed	< dS )

ScoreResponser   scoreband
confidenceloan_ceilinggate_statusexplanationsN
ai_summary)r   r   r
   r   r   r   r   r   r   r%   r   r   r   r   r   r      s   
 r   c                   @   s6   e Zd ZU eed< eed< eed< eed< eed< dS )	GraphNodeidnametrustr   fraudN)r   r   r
   r   r   r   r   boolr   r   r   r   r&   #   s   
 r&   c                   @   s&   e Zd ZU eed< eed< eed< dS )	GraphEdgesourcetargetweightN)r   r   r
   r   r   r   r   r   r   r   r,   *   s   
 r,   c                   @   s&   e Zd ZU ee ed< ee ed< dS )
GraphResponsenodesedgesN)r   r   r
   r   r&   r   r,   r   r   r   r   r0   /   s   
 r0   c                   @   B   e Zd ZU ee ed< eeef ed< eeef ed< eed< dS )FairnessResponsegroupsbefore_disparityafter_disparitynoteNr   r   r
   r   r   r   r   r   r   r   r   r   r4   7   
   
 r4   c                   @   sb   e Zd ZU eed< eed< eed< eed< eed< eed< dZee dB ed< dZ	edB ed	< dS )
r   r   r   r   r   r   r   Nr   requested_loan_amount)
r   r   r
   r   r   r   r   r   r   r;   r   r   r   r   r   @   s   
 c                   @   r3   )r4   r5   beforeafterr8   Nr9   r   r   r   r   r4   M   r:   N)pydanticr   r   typingr   r   r   r   r   r   r&   r,   r0   r4   r   r   r   r   <module>   s    		

</file>
<file path="./config/__init__.py">

</file>
<file path="./config/settings.py">
import os
from dotenv import load_dotenv

# Load the environment variables from the .env file
load_dotenv()

class Settings:
    """Centralized configuration for the TrustLayer Engine."""
    PROJECT_NAME: str = "TrustLayer Engine"
    VERSION: str = "1.0.0"
    API_PREFIX: str = "/api/v1"
    
    # Security & API Keys
    GEMINI_API_KEY: str = os.getenv("GEMINI_API_KEY", "")
    ENV: str = os.getenv("ENV", "development")

# Create a single instance to be imported across the application
settings = Settings()
</file>
</project>